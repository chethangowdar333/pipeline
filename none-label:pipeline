pipeline {
  agent none
    stages {
      stage ('BUILD') {
        agent {label 'slave1'}
      steps {
        echo "This is build stage"
        }
       }
       stage ('DEPLOY') {
         agent {label 'slave2'}
       steps {
         echo "This is deploy stage"
         }
        }
       }
      }
